/* Define struct GroceryItems with fields name and quantity,define a function which accepts a 
 borrowed string as a parameter lets say name and returns Option<i32>, in the function define a 
 vector containing instances of Grocery items, run a for loop over vector and check for the name(use match statement here) 
 if you found the matching name return the quantity in Some else None, 
 in the main match on the function and print the quantity if Some else print “Not in the List” . */


 fn main() {
     
 }