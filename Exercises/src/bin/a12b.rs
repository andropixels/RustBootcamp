/* Define a struct ShoppingList having different  items and impl it for creating a shopping list, define an enum Shops having shops as D-Mart and B-Mart, 
define a associated function over ShoppingList which takes an argument of Shops check with match statement if shop is D-mart then only call the create function. */

fn main() {}